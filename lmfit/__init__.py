"""lmfit — find the biggest local LLM that fits your hardware, and the breakeven
where self-hosting beats the API.

Zero required dependencies: pure stdlib core, with optional `psutil` (better RAM
detection) and `rich` (prettier tables). Everything degrades gracefully.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
